on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@main
            - shell: bash
              run: echo "beginning"
            - name: build-seed-data
              working-directory: ./seed-data
              run: pwd
